{% extends 'layout.html.twig' %}

{% block title %}Carhabty
{% endblock %}

{% block LinksCss %}
	{{ parent() }}
	<link type="text/css" rel="stylesheet" href="{{ asset ('css/popup.css')}}">
	<link rel="stylesheet" href="{{ asset ('css/stylebutton.css') }}">
	<style>
		/***** 🎨 Style amélioré des cartes utilisateurs *****/
		.user {
			background: #ffffff;
			border-radius: 16px;
			padding: 25px 20px;
			width: 260px;
			text-align: center;
			position: relative;
			overflow: hidden;
			transition: all 0.4s ease;
			box-shadow: 0 8px 20px rgba(0, 0, 0, 0.05);
			border: 1px solid rgba(0, 0, 0, 0.04);
		}

		/* Effet lumineux animé au survol */
		.user::before {
			content: "";
			position: absolute;
			top: -50%;
			left: -50%;
			width: 200%;
			height: 200%;
			background: radial-gradient(circle, rgba(255,102,0,0.15) 0%, transparent 60%);
			transform: scale(0);
			transition: transform 0.4s ease;
			z-index: 0;
		}

		.user:hover::before {
			transform: scale(1);
		}

		/* Animation de la carte */
		.user:hover {
			transform: translateY(-8px);
			box-shadow: 0 12px 30px rgba(255, 102, 0, 0.15);
		}

		/* === Icône au centre === */
		.user-icon {
			display: flex;
			justify-content: center;
			align-items: center;
			width: 95px;
			height: 95px;
			margin: 0 auto 18px;
			background: linear-gradient(135deg, #fff5ee, #fff);
			border-radius: 50%;
			box-shadow: 0 4px 10px rgba(255, 102, 0, 0.15);
			position: relative;
			z-index: 1;
			transition: all 0.3s ease;
		}

		.user:hover .user-icon {
			transform: scale(1.08);
			box-shadow: 0 6px 16px rgba(255, 102, 0, 0.3);
		}

		.user-icon i {
			font-size: 2.8rem;
			color: #ff6600;
			transition: color 0.3s ease;
		}

		.user:hover .user-icon i {
			color: #e65c00;
		}

		/* === Texte === */
		.user-name {
			font-weight: 700;
			font-size: 1.15rem;
			margin-top: 10px;
			color: #222;
			letter-spacing: 0.5px;
			position: relative;
			z-index: 1;
		}

		.user-location {
			font-size: 0.9rem;
			color: #666;
			margin-bottom: 15px;
			line-height: 1.4;
			position: relative;
			z-index: 1;
		}


		/* Animation et visibilité des boutons */
		.user-buttons {
			opacity: 0;
			max-height: 0;
			overflow: hidden;
			transition: all 0.4s ease;
			pointer-events: none; /* désactive pendant caché */
		}

		.user:hover .user-buttons {
			opacity: 1;
			max-height: 200px;
			margin-top: 15px;
			pointer-events: auto; /* réactive les clics */
		}


		/* Style général des boutons */
		.user-buttons button {
			width: 85%;
			padding: 10px 0;
			border-radius: 25px;
			font-size: 0.95rem;
			font-weight: 600;
			display: block;
			margin: 8px auto;
			transition: all 0.3s ease;
			box-shadow: 0 3px 10px rgba(0, 0, 0, 0.08);
			cursor: pointer;
		}

		/* Bouton principal */
		.btn-register {
			background: linear-gradient(45deg, #ff6b35, #ff8c00);
			color: #fff;
			border: none;
		}
		.btn-register:hover {
			transform: translateY(-2px);
			box-shadow: 0 6px 15px rgba(255, 107, 53, 0.5);
		}
		.user-buttons {
			cursor: pointer;
		}
		/* Bouton contour orange */
		.btn-outline-primary {
			border: 2px solid #ff6600;
			color: #ff6600;
			background: #fff;
		}
		.btn-outline-primary:hover {
			background-color: #ff6600;
			color: #fff;
			transform: translateY(-2px);
		}

		/* --- Bouton secondaire : Inviter --- */
		.btn-secondary {
			background: linear-gradient(135deg, #2ec4b6 0%, #25a89d 100%);
			border: none;
			color: #fff;
			font-weight: 600;
			letter-spacing: 0.4px;
			box-shadow: 0 4px 12px rgba(46, 196, 182, 0.4);
			transition: all 0.3s ease;
		}

		.btn-secondary:hover {
			background: linear-gradient(135deg, #25a89d 0%, #1f8f86 100%);
			box-shadow: 0 6px 18px rgba(31, 143, 134, 0.5);
			transform: translateY(-3px);
		}


		/* Effet global de survol sur la carte */
		.user {
			transition: transform 0.3s ease, box-shadow 0.3s ease;
		}
		.user:hover {
			transform: translateY(-8px);
			box-shadow: 0 10px 25px rgba(255, 102, 0, 0.25);
		}
		/* === Assure le curseur pointer sur tous les boutons === */
		/* Empêche les éléments décoratifs de bloquer les clics */
		.user::before,
		.user::after {
			pointer-events: none; /* ne bloque plus les clics */
		}

		/* Conteneur des boutons (initialement caché) */
		.user-buttons {
			opacity: 0;
			max-height: 0;
			overflow: hidden;
			transition: all 0.4s ease;
			pointer-events: none; /* désactive les clics tant que caché */
			position: relative;
			z-index: 1;
		}

		/* Quand la carte est survolée, les boutons apparaissent */
		.user:hover .user-buttons,
		.user .user-buttons.active {
			opacity: 1;
			max-height: 220px;
			margin-top: 15px;
			pointer-events: auto; /* clics activés */
		}

		/* Style des boutons */
		.user-buttons button {
			position: relative;
			z-index: 3;
			cursor: pointer; /* le curseur main */
		}

		/* Optionnel : rendre toute la carte cliquable */
		.user {
			cursor: default;
		}
		.user.clickable {
			cursor: pointer;
		}

		/* === 🖥️ Style par défaut (desktop) === */
		.user {
			width: 260px;
		}

		/* === 📱 Sur mobile (moins de 768px) === */
		@media(max-width: 767.98px) {
			.user {
				width: 100% !important;
				max-width: 100%;
				margin: 0 auto;
			}
		}

		/* === 💻 Sur tablette (768px à 991px) === */
		@media(min-width: 768px) and(max-width: 991.98px) {
			.user {
				width: 90% !important;
				max-width: 90%;
				margin: 0 auto;
			}
		}

		/***** users ****/
		.request-card {
			background: linear-gradient(135deg, #ffffff, #f7f8fc);
			border-radius: 15px;
			box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
			padding: 15px;
			transition: all 0.3s ease;
		}

		.request-card:hover {
			transform: translateY(-4px);
			box-shadow: 0 8px 20px rgba(255, 107, 53, 0.3);
		}

		.request-header {
			display: flex;
			justify-content: space-between;
			align-items: center;
			margin-bottom: 10px;
		}

		.request-title {
			font-weight: 700;
			font-size: 16px;
			color: #222;
			line-height: 1.3;
			flex: 1;
		}

		.request-badge {
			background: linear-gradient(135deg, #ff6b35 0%, #fca311 50%, #2ec4b6 100%);
			color: #fff;
			font-size: 12px;
			border-radius: 12px;
			padding: 5px 12px;
			font-weight: 700;
			letter-spacing: 0.5px;
			text-transform: uppercase;
			box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
			display: inline-block;
			transition: all 0.3s ease;
		}

		.request-badge:hover {
			transform: scale(1.05);
			box-shadow: 0 4px 10px rgba(0, 0, 0, 0.25);
		}
		.request-header {
			display: flex;
			justify-content: space-between;
			align-items: center;
			margin-bottom: 8px;
		}


		.request-meta {
			font-size: 13px;
			color: #666;
			display: flex;
			justify-content: space-between;
			flex-wrap: wrap;
			gap: 4px;
			margin-bottom: 10px;
		}

		.request-image img {
			width: 100%;
			height: 220px;
			object-fit: cover;
			border-radius: 10px;
			margin-bottom: 10px;
		}

		.request-description {
			font-size: 14px;
			color: #333;
			line-height: 1.4;
			min-height: 50px;
			margin-bottom: 10px;
		}

		.request-meta {
			display: flex;
			flex-wrap: wrap;
			gap: 10px;
			color: #555;
			font-size: 14px;
			align-items: center;
			margin-bottom: 10px;
		}

		.request-meta i {
			color: #ff6b35;
			margin-right: 5px;
			font-size: 15px;
		}

		.btn-register i {
			margin-right: 8px;
			font-size: 16px;
			color: #fff;
		}
		.btn-register {
			background: linear-gradient(45deg, #ff6b35, #ff8c00);
			border: none;
			color: white;
			font-weight: 600;
			transition: 0.3s ease;
		}
		.btn-register:hover {
			background: linear-gradient(45deg, #ff8c00, #ff6b35);
			transform: translateY(-2px);
		}
		/* Effet au survol */
		.btn-register:hover {
			background: linear-gradient(45deg, #ff8c00, #ff6b35);
			transform: translateY(-2px);
			box-shadow: 0 4px 10px rgba(255, 107, 53, 0.4);
		}

		/* Animation de l'enveloppe quand la carte est survolée */
		.request-card:hover .btn-register i {
			animation: vibrate 0.3s linear infinite;
		}

		@keyframes vibrate {
			0% {
				transform: rotate(0deg);
			}
			25% {
				transform: rotate(10deg);
			}
			50% {
				transform: rotate(-10deg);
			}
			75% {
				transform: rotate(10deg);
			}
			100% {
				transform: rotate(0deg);
			}
		}
		/* 📱 Responsive fixes */
		@media(max-width: 768px) {
			.request-card {
				padding: 12px;
				min-height: auto;
			}
			.request-title {
				font-size: 15px;
			}
			.request-meta {
				font-size: 12px;
			}
			.request-image img {
				height: 180px;
			}
		}


		.btn-propdemande {
			background: linear-gradient(45deg, #ff6b35, #ff9f1c);
			background: linear-gradient(45deg, #2ec4b6, #ff9f1c);
			color: white;
			box-shadow: 0 4px 15px rgba(255, 107, 53, 0.3);
		}


		.btn-offer {
			background-color: #ff4d00;
			color: white;
			border: none;
			width: 100%;
			padding: 10px;
			border-radius: 10px;
			font-weight: 600;
			cursor: pointer;
			transition: background 0.2s ease;
		}
		.btn-offer:hover {
			background-color: #e64300;
		}

		.badge-custom {
			display: inline-block;
			padding: 3px;
			font-size: 11px;
			border-radius: 0 50em 50em 0;
			color: #ffffff;
			font-weight: 600;
			line-height: 23px;
			position: absolute;
			background: linear-gradient(45deg, #ff6b35, #ff9f1c);
			z-index: 1;
			top: 20px;
			box-shadow: 2px 1px 6px 2px rgb(0 0 0 / 10%), 0 4px 4px 0 rgb(0 0 0 / 15%) !important;
		}
		.badge-custom .box {
			height: 26px;
			width: 26px;
			background: linear-gradient(45deg, #ff6b35, #ff9f1c);
			color: #fff;
			display: inline-block;
			border-radius: 50%;
			text-align: center;
		}
	</style>
{% endblock %}

{% block Content %}
<br>

	<div class=" col-lg-12">

		<div class="aiz-carousel dots-inside-bottom mobile-img-auto-height" data-arrows="true" data-dots="true" data-autoplay="true" data-infinite="true">

			<div class="carousel-box">
				<a href="">
					<img class="d-block w-100 rounded" src="https://res.cloudinary.com/aladdineshoping/image/upload/v1762608293/1600w-n13TkX-2HZo_oyan3h.jpg" onerror="this.onerror=null;this.src='assets/img/placeholder-rect.png';" alt="First slide">
				</a>
			</div>
			<div class="carousel-box">
				<a href="">
					<img class="d-block w-100 rounded" src="https://res.cloudinary.com/aladdineshoping/image/upload/v1762608738/1600w-3CEVdzRUSFU_y3zxbz.jpg" onerror="this.onerror=null;this.src='assets/img/placeholder-rect.png';" alt="First slide">
				</a>
			</div>
<div class="carousel-box">
				<a href="">
					<img class="d-block w-100 rounded" src="https://res.cloudinary.com/aladdineshoping/image/upload/v1762608806/1600w-Rgpn1PVgFmA_ddm1ib.jpg" onerror="this.onerror=null;this.src='assets/img/placeholder-rect.png';" alt="First slide">
				</a>
			</div>

		</div>
	</div>
	{% if not app.user %}
	{# categorie parent  #}
	<div id="section_home_categories">
		<section class="mb-4">
			<div class="container">
				<div class="px-2 py-4 px-md-4 py-md-3 bg-white shadow-sm rounded">

					<div class="d-flex mb-3 align-items-baseline border-bottom">
						<h3 class="h5 fw-700 mb-0">
							<span class="border-bottom border-primary border-width-2 pb-3 d-inline-block">
								Choisissez votre rôle
							</span>
						</h3>
					</div>

					<!-- ✅ AIZ Carousel des Profils -->
					<div
						class="aiz-carousel half-outside-arrow gutters-10" data-items="5" data-xl-items="4" data-lg-items="3" data-md-items="2" data-sm-items="1" data-arrows="true" data-infinite="true" data-autoplay="true">

						<!-- Rôle : Propriétaire -->
						<div class="carousel-box">
							<div class="user text-center p-4 shadow-sm rounded bg-white">
								<div class="user-img-wrap mb-3 d-flex justify-content-center align-items-center">
									<div class="user-icon text-primary">
										<i class="fas fa-car fa-3x"></i>
									</div>
								</div>
								<div class="user-meta mb-3">
									<div class="user-name fw-bold">Propriétaire</div>
									<div class="user-location text-muted">نلوّج في قطعة لكرهبتي </div>
								</div>
								<div class="user-buttons mt-3">
									<button class="btn btn-register w-100 mb-2" onclick="location.href='{{path('app_login')}}'">Se connecter</button>
									<button class="btn btn-outline-primary w-100 mb-2" onclick="location.href='{{path('app_register')}}'">S’inscrire</button>
									<button class="btn btn-secondary w-100" onclick="location.href='{{path('inviter_proprietaire')}}'">Inviter</button>
								</div>
							</div>
						</div>

						<!-- Rôle : Mécanicien -->
						<div class="carousel-box">
							<div class="user text-center p-4 shadow-sm rounded bg-white">
								<div class="user-img-wrap mb-3 d-flex justify-content-center align-items-center">
									<div class="user-icon text-success">
										<i class="fas fa-tools fa-3x"></i>
									</div>
								</div>
								<div class="user-meta mb-3">
									<div class="user-name fw-bold">Mécanicien</div>
									<div class="user-location text-muted">  نلوج على قطعة للي يصلح عندي </div>
								</div>
								<div class="user-buttons mt-3">
									<button class="btn btn-register w-100 mb-2" onclick="location.href='{{path('app_login')}}'">Se connecter</button>
									<button class="btn btn-outline-primary w-100 mb-2" onclick="location.href='{{path('app_register')}}'">S’inscrire</button>
									<button class="btn btn-secondary w-100">Inviter</button>
								</div>
							</div>
						</div>

						<!-- Rôle : Vendeur Neuf -->
						<div class="carousel-box">
							<div class="user text-center p-4 shadow-sm rounded bg-white">
								<div class="user-img-wrap mb-3 d-flex justify-content-center align-items-center">
									<div class="user-icon text-info">
										<i class="fas fa-store fa-3x"></i>
									</div>
								</div>
								<div class="user-meta mb-3">
									<div class="user-name fw-bold">Vendeur Neuf</div>
									<div class="user-location text-muted"> نبيع قطايع جدد و نجاوب كل واحد يلوج في قطعة </div>
								</div>
								<div class="user-buttons mt-3">
									<button class="btn btn-register w-100 mb-2" onclick="location.href='{{path('app_login')}}'">Se connecter</button>
									<button class="btn btn-outline-primary w-100 mb-2" onclick="location.href='{{path('app_register')}}'">S’inscrire</button>
									<button class="btn btn-secondary w-100" onclick="location.href='{{path('inviter_vendeurNeuf')}}'">Inviter</button>
								</div>
							</div>
						</div>

						<!-- Rôle : Vendeur Occasion -->
						<div class="carousel-box">
							<div class="user text-center p-4 shadow-sm rounded bg-white">
								<div class="user-img-wrap mb-3 d-flex justify-content-center align-items-center">
									<div class="user-icon text-warning">
										<i class="fas fa-car-side fa-3x"></i>
									</div>
								</div>
								<div class="user-meta mb-3">
									<div class="user-name fw-bold">Vendeur Occasion</div>
									<div class="user-location text-muted">نبيع قطايع قدم (كاس) و نجاوب  كل واحد يلوج  قطعة في الحين</div>
								</div>
								<div class="user-buttons mt-3">
									<button class="btn btn-register w-100 mb-2" onclick="location.href='{{path('app_login')}}'">Se connecter</button>
									<button class="btn btn-outline-primary w-100 mb-2" onclick="location.href='{{path('app_register')}}'">S’inscrire</button>
									<button class="btn btn-secondary w-100">Inviter</button>
								</div>
							</div>
						</div>

						<!-- Rôle : Particulier -->
						<div class="carousel-box">
							<div class="user text-center p-4 shadow-sm rounded bg-white">
								<div class="user-img-wrap mb-3 d-flex justify-content-center align-items-center">
									<div class="user-icon text-danger">
										<i class="fas fa-user fa-3x"></i>
									</div>
								</div>
								<div class="user-meta mb-3">
									<div class="user-name fw-bold">Particulier</div>
									<div class="user-location text-muted">عندك قطعة مفقودة تلوج عليها   أو عندك قطعة زايدة للي يحب يشري     </div>
								</div>
								<div class="user-buttons mt-3">
									<button class="btn btn-register w-100 mb-2" onclick="location.href='{{path('app_login')}}'">Se connecter</button>
									<button class="btn btn-outline-primary w-100 mb-2" onclick="location.href='{{path('app_register')}}'">S’inscrire</button>
									<button class="btn btn-secondary w-100">Inviter</button>
								</div>
							</div>
						</div>

					</div>

				</div>
			</div>
		</section>
	</div>
{% endif %}

	{# fin categorie parent  #}
	{# categorie parent  #}
	<div id="section_home_categories">
		<section class="mb-4">
			<div class="container">
				<div class="px-2 py-4 px-md-4 py-md-3 bg-white shadow-sm rounded">

					<div class="d-flex mb-3 align-items-baseline border-bottom">
						<h3 class="h5 fw-700 mb-0">
							<span class="border-bottom border-primary border-width-2 pb-3 d-inline-block">
								Demandes récentes
							</span>
						</h3>
						<a href="{{path('app_demande_all')}}" class="ml-auto btn btn-register btn-sm shadow-md">Voir plus</a>
					</div>

					<!-- ✅ AIZ Carousel -->
					<div class="aiz-carousel half-outside-arrow gutters-10" data-items="3" data-xl-items="3" data-lg-items="2" data-md-items="2" data-sm-items="1" data-xs-items="1" data-arrows="true" data-infinite="true">

					{% for demande in lastDemandes %}
					 {% if demande.statut != 'fermer' %}
	<div class="carousel-box">
		<div class="request-card">

			<div class="request-header">
				<div class="request-title">
					<a href="{{ path('detail_demande_accuill', {'id': demande.id}) }}" class="text-decoration-none text-dark">
						{% if demande.pieces|length > 0 %}
							{% for piece in demande.pieces %}
								{{ piece.designation }}
								{% if not loop.last %}, {% endif %}
							{% endfor %}
						{% else %}
							{{ demande.marque }} {{ demande.modele }}
						{% endif %}
					</a>
				</div>
				<div class="request-badge">
					{% if demande.vendeuroccasion == 1 %}
						Occasion
					{% else %}
						Neuf
					{% endif %}
				</div>
			</div>

			<div class="request-meta">
				<span>
					<i class="fas fa-map-marker-alt"></i> {{ demande.zone }}
				</span>
				<span>
					<i class="fas fa-user"></i> {{ demande.offrecompte ? demande.offrecompte.nom : 'Anonyme' }}
				</span>
				<span>
					<i class="far fa-clock"></i> {{ demande.datecreate|time_ago }}
				</span>
			</div>

			<div class="request-image">
    <a href="{{ path('detail_demande_accuill', {'id': demande.id}) }}">
        {% set firstPiece = demande.Pieces|first %}
        {% set photo = firstPiece and firstPiece.photo ? firstPiece.photo : '/image/placeholder.png' %}
        <img src="{{ photo }}" alt="photo" class="img-fit mx-auto rounded">
    </a>
</div>


			<p class="request-description">
				{% if demande.pieces|length > 0 %}
					{% for piece in demande.pieces|slice(0,3) %}
						{{ piece.observation }}
						{% if not loop.last %}, {% endif %}
					{% endfor %}
					{% if demande.pieces|length > 3 %}...{% endif %}
				{% else %}
					Pas de description disponible.
				{% endif %}
			</p>


		{% set dejaPropose = app.user ? (demande.offres|filter(o => o.user.id == app.user.id)|length > 0) : false %}
{% set estVendeur = app.user and 'ROLE_VENDEUR_NEUF' in app.user.roles %}

<a href="{% if estVendeur and not dejaPropose %}{{ path('propose_offre', {'id': demande.id}) }}{% else %}#{% endif %}" 
   class="btn btn-register btn-circle w-100 text-center"
   {% if not app.user %}
       onclick="AIZ.plugins.notify('danger', 'Veuillez vous connecter pour proposer une offre !'); return false;"
   {% elseif not estVendeur %}
       onclick="AIZ.plugins.notify('danger', 'Seuls les vendeurs peuvent proposer une offre !'); return false;"
   {% elseif dejaPropose %}
       onclick="AIZ.plugins.notify('danger', 'Vous avez déjà proposé une offre pour cette demande !'); return false;"
   {% endif %}>
    <i class="fas fa-envelope-open-text me-2"></i>
    Proposer un prix
</a>

		</div>
	</div>
	{% endif %}
{% else %}
	<p class="text-center text-muted">Aucune demande récente.</p>
	
{% endfor %}
</div>
				</div>
			</div>
		</section>
	</div>

	{# fin categorie parent  #}
{% endblock %}
{% block javascript %}
	{{ parent() }}
	<script type="text/javascript"></script>
{% endblock %}

