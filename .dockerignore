# Ignorer le contrôle de version
.git
.gitignore

# Ignorer les dépendances locales (vendor sera généré dans le conteneur)
# node_modules peut rester ignoré si frontend build ailleurs
node_modules

# Ignorer les caches et logs Symfony
var/cache
var/log
var/sessions

# Ignorer les fichiers de configuration locaux
.env.local
.env.test
.env.dev
.env.backup
.env.example

# Ignorer les fichiers temporaires ou système
*.log
*.tmp
*.bak
*.swp
.DS_Store
Thumbs.db

# Ignorer les fichiers Docker inutiles
docker-compose.yml
Dockerfile

# Ignorer seulement le build frontend, pas tout public
/public/build
/public/node_modules
